NODE_ENV=development
PORT=5000

# Prisma MySQL connection string
DATABASE_URL="mysql://root@localhost:3306/dhanvantri_clinic"

# JWT
JWT_SECRET="change-this-to-a-strong-secret"

# Frontend URL (used in email links / CORS in some places)
FRONTEND_URL="http://localhost:5173"

# SMTP (optional for local; mail service falls back to logs if not valid)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="\"Exclusive Vision\" <otp@exclusivevision.com>"
